---
name: arch-case-collector
description: 建筑设计案例搜集器。输入一个主题（类型/材料/地域/建筑师），按来源分级检索互联网 → 两道闸门筛选 → 产出符合 Arch_Vault 案例库整理规范的「快速收录」MD 笔记 + 图片本地化 + 搜集回执。触发词：搜集案例、搜集、找案例、案例搜集、collect cases、补充案例库、收录案例。
---

# arch-case-collector · 建筑案例搜集 Skill

> 职责：主题 → 一批合规案例笔记 + 搜集回执。**只做「快速收录」档，不做深度归档。**
> 设计蓝图：`C:\Users\DK\WorkBuddy\建筑skill\案例搜集skill-设计蓝图.md`（本 skill 的一切决策以其为准）。
> vault 规范（硬约束）：`<vault>/00 - Meta/案例库整理规范.md`——执行前必读，本文与其冲突时**以规范为准**。

> **来源优先级总序**：`S 事务所官网/官方（最高） > A 专业建筑媒体（gooood/有方/ArchDaily/Dezeen…） > B 中文一手社区媒体（微信公众号 / 小红书） > C 百科/索引/讨论（Wikipedia/知乎/Pinterest…）`。
> 微信公众号、小红书是**正式可回源来源**：可检索、可核验、可收录（走 §5.a），只是优先级**低于** S 与 A——**优先搜官网与专业媒体，信息不足时才降级到公众号/小红书补齐**，而非反过来。
> 知乎/Pinterest 仅作灵感线索，事实须回源到 B 级及以上。

## 0. 关键路径（config.yaml 为准）

- vault：`U:\DK_iCloud\iCloudDrive\iCloud~md~obsidian\Arch_Vault`
- **归档位置（默认 vault，写前必读 §6.a）**：建成案例 → `<vault>/01 - 案例库/<type>/`；**专题研究 / 产品研究类 → `<vault>/06 - 专题研究/<专题名>/`**；图片统一 → `<vault>/attachments/Clippings/<项目名>/`。**WorkBuddy 资料库仅在用户显式点名时才写。**
- 本 skill 目录：`C:\Users\DK\WorkBuddy\建筑skill\.workbuddy\skills\arch-case-collector`
- 暂存区：`C:\Users\DK\WorkBuddy\建筑skill\staging\<批次名>\`
- 自检清单：`assets/checklist.md`；来源清单：`assets/sources.csv`
- 复用脚本：`scripts/xhs_scrape.py`（给定小红书笔记 URL 列表批量取图）、`scripts/xhs_search.py`（小红书关键词 → 项目名收敛 → 自动逐笔记访问 → 自动落图 + 生成 `raw/xhs_digest.md` 文案摘要）、`scripts/wx_search.py`（**微信公众号**：多关键词 → 搜狗结果过滤 → 逐篇页内跳转打开 → 抽正文与图 → 下载 + `raw/wx_digest.md`，见 §2.d.10）、`scripts/wx_dl.py` / `scripts/wx_from_article.py`（微信图批量下载 / 从文章快照下载）、`scripts/sheet_any.py`（**通用审图板**：任意目录 → 带编号拼板，供逐张核验）、`scripts/audit_sheet.py`（**图片内容审图板生成器**：把各项目目录图片拼成带编号的拼板，供逐张视觉核验；`python audit_sheet.py HT|ALB|全部`）、`scripts/reclassify.py`（按核验结论批量改名重建 `HT/ZZ/POSTER/ALB` 分类的模板，改 map 即用）、`scripts/dump_cases.py`（把 `make_list.py` 的案例数据 + 磁盘图片实况导出为 `raw/cases_dump.json`）、`scripts/to_vault.py`（**把本批素材归档进 vault**：按 `PLAN` 表复制到 `attachments/Clippings/<项目名>/NNN-*.ext`，走「写 tmp → md5 校验 → 原子改名」，见 §6.b）、`scripts/push_to_library.py`（**仅在用户点名资料库时用**：读 `raw/cases_dump.json` → 建 Database + 批量写记录，见 §6.c）、`scripts/plan_assets.py`（按项目生成带编号审图板 + `raw/asset_plan_raw.json`，供归档前逐张核验与命名）、`scripts/board_new.py`（**按显式清单**拼带编号审图板：读 `raw/new_files.txt`（每行 `项目key/文件名`）→ `raw/boards/<前缀>_boardN.jpg`，用于补轮次只核验新增图）、`scripts/to_vault2.py`（**补轮次归档模板**：`PLAN` 为 `(项目名, 源key, 起始编号, [...])`，新编号追加在既有最大编号之后，既有文件一个不动）、`scripts/verify_vault.py`（**落库验收**：核 vault 笔记全部 `![[...]]` 是否命中 + frontmatter 可解析 + 列出「已归档但未被笔记引用」的漏网图）、`scripts/verify_list.py`（核 `候选清单.html` 的 `img/` 引用是否命中 + 列出未引用图）、`scripts/bsk_run.py`（browser-skill 稳健调用器）；用法见各自顶部 docstring。**登录墙来源取图、图面核验、vault 归档，优先调用它们，不要每次重写。**
- ⚠️ **本机执行环境陷阱（2026-09-15 实测，必读）**：
  1. **shell 的 PATH 会坏掉**（`bash: mkdir/dirname/head/cat/tail: command not found`）→ **不要依赖 shell 工具链**：文件操作一律用 **Python 绝对路径**（`C:\Users\DK\.workbuddy\binaries\python\versions\3.13.12\python.exe`），不要 `cd` 到含中文的目录再执行命令。
  2. **browser-skill 的 daemon 会被连带清理**：若 daemon 由普通命令内的 `bsk status/…` 懒启动，**命令一结束它就被回收**（日志只有 `daemon ready` 没有 shutdown），于是**会话建了就丢**（`session not registered`）。→ **必须用常驻后台任务启动**：`bsk daemon stop; bsk daemon start --foreground`（`run_in_background=true`），再 `bsk session start`，此后会话稳定。
  3. `bsk wait-ms` **不接受 `--session`**（它是 daemon 侧全局命令）：写 `bsk wait-ms 4000`。
  4. Python 里调 bsk 要用**裸名 `["bsk", ...]`**（`shutil.which` 能解析）；用 `.local\bin\bsk.exe` 绝对路径会被拦。
- **抓取方式铁律（fetch 分流）**：`sources.csv` 的 `fetch` 列决定每个来源的默认抓取通道——`web`=curl/WebSearch/WebFetch 直读；`browser`=**默认 browser-skill 操作浏览器**（登录墙/需 JS 渲染来源：小红书、Pinterest、微信公众号正文、知乎）。执行时按此分流，**不逐案纠结**。

## 1. 触发与选项解析（SOP ①②）

用户说「搜集 N 个 <主题>」即触发。解析下列前置选项（用户未提的取 config.yaml 的上次值，首次取默认）：

| 选项 | 默认 | 说明 |
|---|---|---|
| `query` | 必填 | 自然语言主题 |
| `count` | 5 | 目标正式收录篇数 |
| `type_hint` | 自动判定 | 判定到规范 §3 词表后**必须与用户确认一次**（type 决定文件夹） |
| `region` / `year_range` | 不限 | 地域 / 年代过滤 |
| `source_lang` | 中英混合 | 中文为主 / 国际为主 / 混合 |
| `source_on` | 全开 | 各级来源可单独关闭 |
| `use_browser` | auto | auto=有登录墙来源（小红书/Pinterest/微信公众号/知乎）**默认走 browser-skill**，无登录墙来源用 curl/WebSearch；可 always（全部强制浏览器）/ off（全部禁浏览器，登录墙来源则跳过并在回执注明） |
| `mode` | standard | standard ≥70 收 / strict ≥80 / loose ≥60 |
| `pref` | **关** | 临时偏好（建筑师名单/关注点/排除项），仅作相关度加分；**不内建任何研究画像** |
| `confirm` | 开 | 落库前先出候选清单等用户确认 |
| `report_to` | `00 - Meta/搜集回执/` | 回执去向，可改工作区或关闭 |

## 2. 检索（SOP ③）

1. **发现轮**：对 `<vault>/01 - 案例库` 现有类型词表 + 主题关键词做 WebSearch，扫 sources.csv 中启用站点。
2. **核验轮**：候选项目回到更高级别来源核验事实字段（建筑师/年份/地点/面积）。
   - **字段冲突处理**：同一字段在不同来源冲突时，按来源级别 S>A>公众号·小红书>百科>线索 取值，并在笔记正文以 ⚠️ 标注双方口径与可能原因（如统计范围不同），同时在回执「字段冲突与待核实」列出。**不擅自取其一、不静默择一**。常见冲突：事务所供稿面积 vs 政府/百科口径；媒体发布年份 vs 实际建成年份。
3. **题图抓取**（核验轮内顺手完成，不额外多轮）：为每个入围候选确定 **1 张题图**——选来源文章中信息量最大的一张（优先鸟瞰/全景主图，其次正文首图），记下 URL 与出处。核验轮结束、用户确认前，统一下载到 `staging/<批次名>/候选题图/NN-<项目短名>.jpg`。取不到可用题图的候选 → 清单中标注「无题图」并注明原因（完整度评分本就会反映）。
4. **抓取降级链**（顺位检索：高→低，信息不足以覆盖时往下走）：`WebSearch/WebFetch → S 事务所官网/官方 → A 专业媒体 → B 微信公众号/小红书 → C 百科/索引 → 仍失败则在回执注明「该来源未覆盖」，**不硬撑、不编造**`。
   - **抓取方式按「是否登录墙」分流（铁律，不再逐案纠结）**：
     - **无登录墙来源**（S 事务所官网、A 专业媒体、C 百科/索引等）→ 直接 `curl`/`WebSearch`/`WebFetch`。
     - **有登录墙/需 JS 渲染来源**（**小红书、Pinterest、微信公众号正文、知乎**）→ **默认走 browser-skill 操作浏览器**（登录态读取），而不是先 curl 硬试。对应关系以 `assets/sources.csv` 的 `fetch=browser` 列为准。
     - 唯一例外：微信公众号正文若已被搜索引擎索引，可先 `WebSearch` 拿到全文摘要定位，但**要抓正文/图片时仍走 browser-skill**；小红书/Pinterest 站内内容几乎只能靠浏览器登录态。
5. **gooood 高效用法（已实测，优先采用）**：
   - **按类型取最新项目**：直接 curl gooood 分类页，比逐条 WebSearch 快得多。例（产业园）：`https://gooood.cn/type/%25e4%25ba%25a7%25e4%25b8%259a%25e5%259b%25adindustrial-park`，然后 `grep -oE '(href|url)="[^"]*\.htm[^"]*"'` 提 slug；其他类型替换 type slug 即可。分类页自带发布日期，便于按「越新越好」排序。
   - **正文图 URL 规律**：`https://oss.gooood.cn/uploads/<发布年月>/<编号>-<slug>-<宽x高>.jpg`，按 slug 过滤即可精准提取正文图；`-150x150` 为缩略图须排除。curl 需带 UA + `-e https://www.gooood.cn/`。
   - **项目信息表被 JS 转义**：直接 grep 拿不到，先解码再提字段——`sed 's/\\u003C/</g; s/\\u003E/>/g; s/\\u002F/\//g; s/\\u0022/"/g'`，再 grep `建成时间|建筑面积|用地面积|业主|摄影`。页面无信息表时，用 WebSearch 命中微信公众号转载全文（通常含完整项目信息卡）。
   - **图纸序列**：多数项目另有 `NNN-<slug>-drawing-<宽x高>.jpg` 系列，值得一并抓取。
6. **图片抓取限流教训**：`images.adsttc.com`（ArchDaily CDN）对高频 curl 触发 **IP 级 AccessDenied，冷却期 >10 分钟**，且返回 200 状态码导致 `--retry` 不生效（须 `curl -f` 才触发重试）。**默认优先走 gooood / 事务所官网图源**；确需 ArchDaily 时每张间隔 ≥10s。
5. browser-skill 用法（**沙箱环境关键：daemon 必须前台常驻才能跨命令存活**）：
   - **前置两条硬命令**（每次开跑前必做，否则 session 在下一命令即失效）：
     1. `bsk daemon start --foreground` 用 `run_in_background=true` 跑（让 daemon 成为后台任务的直接进程，而非 detach 子进程）；
     2. 之后每条 `bsk` 命令都加 `dangerouslyDisableSandbox=true`（沙箱内 daemon 每命令即起即灭、uptime 恒 0s，session 无法保持）。
   - 验证就绪：`bsk status` 的 `uptime` 应**持续增长**（而非恒 0s）、`browsers connected ≥ 1`；`bsk session start` 后隔一条命令 `bsk session list` 仍能看到该 session 即成功。
   - 升级：`bsk update` 依赖 GitHub（`github.com/Tencent/BrowserSkill/releases/download/cli-<ver>/bsk-<ver>-x86_64-pc-windows-msvc.zip`），**沙箱内 GitHub 不可达**；脱离沙箱 + VPN 后直连可下（手动 `curl -sL -o` 后解压替换 `~/.local/bin/bsk.exe`，旧版先 `cp` 备份）。0.1.10 的 daemon 即使脱离沙箱也无法常驻，**需升到 0.2.0**。
   - 红线不变：只读浏览（不点赞/收藏/关注/发布）；遇验证码/登录失效交还用户；有界会话，目标达成即关（`bsk session stop <id>` + `bsk daemon stop`）；限频，不批量爬；仅个人学习用途。
6. **C 级来源（Wikipedia/百度百科/知乎/Pinterest 等）** 仅作线索与概览：事实须回源到 **B 级及以上**（B=公众号/小红书，更理想是 S/A）。**B 级（公众号/小红书）不是纯线索**，可检索、可核验、可作为正式笔记事实/图片来源并收录，优先级见本条上方总序。
   - **但收录规则有区分**：纯靠公众号/小红书单源建成的案例 → 该篇在回执标记来源=B级、图源=社媒（watermark），`status` 仍 `待整理`；若属"无从回源 S/A 的一手首发信息"（如小红书甲方一线造价帖）则连同特征一起如实收录并在正文标注平台出处，**不因是社媒就放弃，也不臆造更高来源**。
   - **已知取舍**：社媒图片多带水印/经压缩、可能非版权持有方授权，故图片质量与授权都低于官网/专业媒体；凡能回源到 S/A 高清原图的一律先回源。

## 2.a 专题式检索（推荐模式，已实测）

适用场景：用户给出"主题 + 特征偏好"（如"产业园区 + 外立面成本控制"），不是单纯类型检索。

1. **先定特征标签**（如 feature/材料表达、feature/造价控制、feature/模数化），把"专题"翻译为受控词表的 1~3 个标签。
2. **用特征做反向检索**：以标签组合作为检索词，回到 sources.csv 中 A/B 级站点 grep 案例；优先取在文中**显式提到造价/材料对比/全幕墙弃用**的稿件，跳过只描述效果不提做法的稿件。
3. **多源互补**：A 组通常 gooood 全覆盖（产业园类型页效率最高）；B 组在 gooood 占满后顺位回退——`archdaily → gooood → 有方 → ARCHINA → 知末 → bim5 → 网易/搜狐门户转发 → 事务所官网`。对应站点返回 403/404/懒加载未开放直链时，直接落文字档案（见 §5.a）。

## 2.b 限频（HTTP 429）应对

- 响应里的 `Retry-After` 或 `X-RateLimit-Reset` 给出 reset 时间，本地时间换算参考：
  - Bash：`date -d @<unix> '+%H:%M:%S %Z'`（Linux/Git Bash）。
  - PowerShell：`[DateTimeOffset]::FromUnixTimeSeconds(<unix>).LocalDateTime`。
- 短任务（≤2 小时）等待；长任务立即切到备用图源（见 §2.a 第 3 步）。
- **绝不降级到次级来源的伪造数据**；如切到备用源仍受限，把笔记写到 staging 不强制落库，等下次窗口再补图。

## 2.c B 级来源（微信公众号 / 小红书）——检索与抓取要点

> 定位：**中文一手社区媒体**。微信公众号常为事务所官方转载/首发（接近 A 级可信度）；小红书是一线实操情报（材料做法、造价、甲方视角）密度最高处。优先级低于 S/A，但**正式可回源**。

1. **何时主动启用**：① A 级站点 403/404/懒加载拿不到该案例（落 §2.a 顺位时顺带试 B）；② 主题偏"做法/造价/实操"这类专业媒体叙事弱、社媒一线情报强的内容；③ S/A 已给足但想补小红书/公众号特有的"造价对比/材料坑"细节。
2. **检索方式**：
   - **登录墙来源（小红书/Pinterest/微信公众号正文/知乎）→ 默认 browser-skill**：先 `bsk status` 确认就绪，再 `bsk navigate <搜索/详情 URL>` → `bsk snapshot` 读取站内内容。不要先 `curl` 硬试——这些站的正文/详情在登录墙后或需 JS 渲染，curl 拿到的是空壳或反爬页。
   - 仅作**定位辅助**时可用 WebSearch 找站外可见摘要/链接（`site:mp.weixin.qq.com <主题>` / `site:xiaohongshu.com <主题>`），拿到标题与入口后，**正文/图片仍回 browser-skill 抓**。
   - 红线见 SKILL.md §2 第 5 条：只读、不点赞/发布、遇验证码交还用户、限频。
3. **判断是否"一手可证"**：公众号看是否**事务所官方认证号/主创署名**（可信度≈A）；小红书看是否有**可追的实体项目名 + 场景照片 + 设计/施工方署名**（可信度中）。纯转发、无署名、营销号拼贴 → 降为 C 级线索处理。
4. **回源优先**：文中提到的项目名/事务所 → 回 S/A 核验字段、取高清原图；**能回源的一律回源**。
5. **无法回源的一手首发**（例如小红书某甲方/材料商发的特定项目造价明细，S/A 检索不到）：按 §2.c 收录规则如实收录（正文标注平台来源与"供参考"），不臆造事务所/年份等更高字段——缺失字段留空并在回执「字段冲突与待核实」记录。
6. **图片**：社媒图带水印 → 作为候选/补充素材而非首图优先；同一项目优先用官网/专业媒体图做 `cover` 与正文图。

### 2.c 实测结论（2026-09-05 补，两个渠道可达性不同，务必区别对待）

> 经验来源：一次「纯 B 级来源」专项测试（只从公众号+小红书搜"产业园区外立面成本控制"）。

1. **微信公众号 = 可回源的有效 B 级（正文默认走 browser，WebSearch 仅定位）**：搜索引擎会索引公众号文章的标题/部分正文，`WebSearch` 可先拿到摘要定位到具体文章；**但要抓完整正文与图片、核验字段时，默认走 browser-skill 打开 `mp.weixin.qq.com` 原文读取**（登录墙/JS 渲染下 curl 拿不全）。**注意**：公众号大多是 A 级媒体（gooood/有方等）稿件的二次分发，纯公众号检索能捞到的优质案例**绝大多数已在之前批次经 A 级来源入库**——用公众号"挖全新案例"价值有限，其真正价值在于**补字段（A 级页面无信息表时）与补一线造价细节**。
2. **小红书 = 纯 Web 检索基本失效，但浏览器登录态已跑通（2026-09-05 验证）**：站内笔记有登录墙 + 反爬，加 `site:xiaohongshu.com` 直搜只会返回百度百科/爱采购/材料商 SEO 文（"铝板多少钱一平"类），**拿不到站内一线项目帖**。正确路径是 browser-skill 登录态：`bsk daemon start --foreground`（后台）→ `bsk session start` → `bsk navigate https://www.xiaohongshu.com/search_result?keyword=<URL编码主题>` → `bsk snapshot`。实测命中一线帖如「造价3000元/㎡的产业园项目实践」「weico项目|1900元/㎡预算应该花在哪里」「weico设计|幕墙的逻辑」——**小红书确实是"造价/材料做法"一类实操情报密度最高的渠道，官方号（如 weico 建筑事务所）可信度≈A**。
3. **browser-skill 就绪性检查前置（登录墙来源的默认第一步）**：凡本轮需访问小红书/Pinterest/微信公众号正文/知乎（fetch=browser）前，**先 `bsk daemon start --foreground`（后台）+ `bsk status` 确认就绪**；若返回空/SIGTERM 或显示 standby/未连接，说明扩展未连——**不硬等、不硬撑、不退回 curl 硬试登录墙**，本轮直接按"该渠道未覆盖"处理，并在回执如实记录"该渠道因浏览器未连接未覆盖"。
4. **可达性优先级实操序（按登录墙分流，勿混淆）**：`无登录墙（S/A/C 百科）→ curl/WebSearch 直读`；`有登录墙（小红书/Pinterest/微信公众号正文/知乎）→ browser-skill 登录态`。两者不是"谁先谁后"的替代关系，而是按 `sources.csv` 的 `fetch` 列**各自走各自默认通道**；同一主题可先扫无登录墙来源，再视主题需要走浏览器登录态捞社媒一线情报。
5. **daemon 保活（2026-09-07 实测，重要）**：命令行里每条命令跑完会被回收子进程 → bsk daemon 每次重启、会话即丢（症状：`session start` 打出 id 但 `session list` 为空 / `navigate` 报 "session not registered"）。解法：**用后台任务方式 `bsk daemon start --foreground` 常驻**（本环境即工具级 run_in_background），之后每条 bsk 命令连同一 daemon（`status` 的 pid/uptime 持续即成功）。另：Git Bash 下 `bsk xx | head` 会偶发 SIGTERM，**输出一律 `> 文件` 后读文件**。**daemon 空闲 600s（10 分钟）会自动退出**——这是正常设计，不是崩溃；下次用前 `bsk status` 会自动拉起，但**旧 session 会丢**，需重新 `bsk session start`。**bsk 自动更新需重启 daemon 才生效**（日志会打 `auto-update staged … cannot replace the running daemon in place; restart`），如需升版本须先 `bsk daemon stop` 再重起。
6. **小红书取图路径（2026-09-07 首测，2026-09-15 修正为直下可用）**：① 用户态**搜索页可用**（`search_result?keyword=<url编码>`），卡片 = `section.note-item` / `.note-item`，内含标题、`data-note-id`、`a.cover` 链接；② 详情页**直接 `/explore/<id>` 会 404**，必须取卡片 **`a.cover` 的 href**（`/search_result/<id>?xsec_token=…`）导航打开；③ **取图（2026-09-15 关键修正）**：正文图就在页面 `img` 里，**认 URL 后缀 `!nd_dft_wlteh_webp_3`（＝详情页大图）**，**原样带后缀 + `Referer: https://www.xiaohongshu.com/` 即可直下 1080×1440 原图**。⚠️ 后缀参与 CDN 签名——**替换后缀、或去掉后缀，一律 403**（旧结论「CDN 直链一律 403」不准确）。旧法「覆盖层+整页截图+PIL 裁黑边」仅在直下 403 时作兜底；④ 会话结束时 `bsk session stop <id>` + `bsk daemon stop`。现成脚本见 `scripts/xhs_scrape.py` / `scripts/xhs_search.py`。
7. **⚠️⚠️ 图片「取到」≠「可用」——必须逐张核验图面（2026-09-15 血的教训，全局强制）**：
   - **踩过的坑**：一批 10 个楼盘、从小红书取回 40 余张图，全部按**笔记标题**归类为「户型图」；逐张目视核验后发现**标题普遍与图面不符**——写「全套户型图／全户型首发」的笔记，正文常常整篇是会所与景观实景照；确有其图的，也可能被发布方**主动打码**，或只做成海报底部的**极小缩略图**。最终该批可计入户型图的 **0 张**，此前按标题计为 11 张属虚报。
   - **强制规则**：任何图片下载入库后，**必须逐张目视核验图面内容，再决定归类**。归类只允许落成四种前缀：
     `HT`＝户型图（平面图）｜`ZZ`＝总图／规划鸟瞰／审批版鸟瞰／沙盘模型｜`POSTER`＝营销海报（含打码或缩略平面，**不得计入 HT**）｜`ALB`＝楼盘图（效果图／实景／景观／室内）。
   - **做法**：用 `scripts/audit_sheet.py` 生成审图板（每板 9 格、带 `[序号] 文件名` 标签），**分板读图**再判定——比逐张读原图省几十倍 token。原判「是户型图」的与「不是户型图」的**两个方向都要查**（反向查才能捞出被埋掉的户型图）。
   - **禁止**：把「按标题／按门户栏目」的归类**直接**写成 `HT` 并计入素材统计；未核验的只能标为待核。
   - **附带收益**：反向审图常能捞出被误归的**总图级素材**（规划鸟瞰总图、带「批准许可证号」的审批版鸟瞰、实体沙盘照）——这正是「总图」类需求的答案。
   - **顺带识伪**：与项目产品形态明显不符的图（叠墅盘里出现 CBD 超高层夜景、滨海度假小镇、带无关 logo 的办公楼效果图）多为**门户相册混入的图库占位素材**，须标注「疑图库占位」并**不得当作项目实景**。

### 2.d 销售期住宅/商办类主题（2026-09-15 实测，与建成案例主题差别很大，务必先读）

> 适用：主题是「**在售/在推楼盘的产品资料**」（叠墅、合院、户型、总图、产品线、造价、去化…），
> 而非「某事务所建成的建筑作品」。这类主题**不能用建成案例的来源分级与检索路径硬套**。

1. **来源分级必须先与用户确认「产品研究口径」**：销售期楼盘在 S 级事务所官网 / A 级专业建筑媒体上**几乎无覆盖**（营销主体是开发商，不是建筑师）。主力来源实为：**楼盘官方信息页 / 房产门户（吉屋、搜狐焦点、房天下、安居客）/ 微信公众号楼盘解读 / 政府规划公示**。
   → 若沿用 S100/A85/C40 的分级，几乎全部候选会被压到候选线以下、一条都收不进来。**执行前必须显式提出并确认**新增一档：
   `营销资料档 = 60`（房产门户的楼盘官方信息页、开发商公示口径）、`专业机构报告 = 80`（克而瑞等）、`政府规划公示 = 95（S 级）`、`党媒/港媒地产版 = 85（A 级）`。
2. **政府规划批后公布 = 「总图」类需求的最优且唯一 S 级来源**（本次实测最有价值的一条）：
   - 站点：`https://ghzyj.gz.gov.cn`（广州市规划和自然资源局）。
   - 批后公布详情页形如 `/ywpd/cxgh/ghxkgsgb/phgbnew/gbt/<年>/content/post_<id>.html`，页内含「点击查看盖章图」→ 指向 `/attachment/.../<id>.pdf`。
   - 该 PDF 通常是**单页**，内嵌 1 张 1191×860 的**总平面示意图**（带审批盖章、含各项规划指标）。用 PyMuPDF 渲染：`page.get_pixmap(dpi=260~300).save(out)`，得到 4300×3100 级别的高清总图。
   - 检索方式：`WebSearch "ghzyj.gz.gov.cn 批后公布 <地块编号/案名> 总平面"` 命中率很高。
   - ⚠️ **PyMuPDF 的 `pixmap.save()` 不认含中文的**相对路径，**必须传绝对路径**，否则报 `cannot open file ... No such file or directory`。
   - ⚠️ 老域名 `ghryj.gz.gov.cn`（无 y）已 502，用 `ghzyj.gz.gov.cn`。
3. **吉屋 `gz.jiwu.com` 是「户型图 + 楼盘图」最有效的直抓源**（robots 允许 `/lpcp/` 与 `/news/`，禁止 `/esf/`、`/huxingp/`、`/sousuo/`、`/loupan/list-*` 等，勿越界）：
   - 项目详情页：`https://gz.jiwu.com/lpcp/<id>.html`；也有移动版 `https://m.jiwu.com/gz/loupan/<id>.html`。
   - 图片路径可分类：**`/housetype/` = 户型图**，**`/album/` = 楼盘图（效果图/实景/鸟瞰）**。
   - ⚠️ **同一项目常有多条 lpcp（重复条目）** → 户型图会重复抓到，**必须按内容 MD5 去重**。
   - ⚠️ **`/album/` 中大量是「售楼处电话」广告位**（红色横幅、无信息量）→ 过滤规则：**宽高比 > 3.2** 判广告；另可统计**跨项目重复出现的哈希**判站内通用图。
   - ⚠️ 各盘户型图覆盖**不均**（本次 10 盘中仅 5 盘有）。**缺图先走 §2.d.7 贝壳新房通道（免登录，实测能补），仍缺再走 browser-skill 登录态**（小红书/微信公众号）。
4. **UA 陷阱（本次踩坑，通用）**：UA 中含 `KHTML, like Gecko` 会被房天下/焦点等站点风控，返回 800 字节的 `checkyzm` 验证码页（**HTTP 200，不报错，极易误判为"页面没数据"**）。
   → 统一使用短 UA：`Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120 Safari/537.36`。
   → 另：`python urllib` 在这些站点上会被拦（TLS 指纹），**改用 curl 子进程**（`C:\Windows\system32\curl.exe`）才通。
5. **焦点网 `focus.cn` 图片为 JS 懒加载**，curl/WebFetch 只拿到 1 张占位图 → **不要指望从其正文提图**，只当文字来源用。房天下的 `search` 接口**不按 keyword 过滤**（永远返回同一批推荐盘），拿不到目标 projid，暂不可用于自动定位。
6. **已实测确认「免登录拿不到」的通道（2026-09-15，勿再重复试）**：
   - **百度图片** `image.baidu.com/search/acjson` → 返回 `{"antiFlag":1,"message":"Forbid spider access"}`，需 cookie 指纹。
   - **搜狗微信** `weixin.sogou.com/weixin?type=2&query=<url编码>`：⚠️ **2026-09-15 修正**——原文记「跳转被 antispider 拦死、只能当线索」**只对「无 cookie 的 curl 直连」成立**。在 **browser-skill 已连通的浏览器**里，条目 `/link?url=` **可以完整跳通到 `mp.weixin.qq.com` 文章**，图文均可取（见 §2.d.10）。故搜狗微信既是发现轮，也是可用图源通道。
   - **贝壳移动端 `m.ke.com`** → 返回 `CAPTCHA` 页。**安居客** → 需登录。
7. **贝壳新房 PC 站 `gz.fang.ke.com` = 免登录的「开发商正式户型图」通道（2026-09-15 打通，优先于浏览器登录态尝试）**：
   - **可用**：楼盘详情页 `https://gz.fang.ke.com/loupan/p_<id>/`（200，约 220KB）、列表页前 3 页 `https://gz.fang.ke.com/loupan/pgN/`、**站点地图** `https://gz.fang.ke.com/sitemap-new-resblock.xml`（→ `sitemap-new-resblock-1.xml`，含全库约 **838 个**广州楼盘 URL）。
   - **图片分类（关键）**：详情页内 `ke-image.ljcdn.com` 的图分三组——
     **`hdic-frame` = 开发商正式户型图**（图面带项目名、面积、房型编号、房间标注，无门户水印，**可直接入库**）；
     `newhouse-user-image` / `hdic-resblock` = 相册实景/效果图（带"贝壳找房"斜纹水印，**且实测存在图库占位素材风险**——绿城·臻园该组首图竟是"纽约中央公园天际线"照 → **一律不采信为项目实景，不入库**）。
   - **取高清**：URL 尾部 `.690x.jpg` → 改 `.2000x.jpg`（实测 70KB→365KB）。直接去掉后缀会 403。
   - **按名定位楼盘**：PID 前缀 = **楼盘名拼音首字母**（如 `p_ntmyaaufh`＝南天名苑、`p_blhgysbmrjv`＝保利湖光悦色、`p_lczyboffo`＝绿城·臻园）。做法：拉 sitemap → 用各目标盘的拼音缩写前缀筛候选（如 `lczy`/`hcyhy`/`ntmy`）→ 逐个 GET 详情页读 `<title>` 确认身份。
     ⚠️ 前缀会**误命中**（`yxzs` 同时匹配"御溪臻山墅"与"越秀珠实"系列），**必须读 title 复核**；HTTP 200 但 1127 字节且 title 为「登录」＝撞登录墙；title 为「CAPTCHA」＝触发风控，退避重试。
   - **受限项**：户型图专页 `/loupan/p_<id>/huxingtu/`、列表第 4 页起、搜索接口 `?keyword=` 全部**需登录** → 所以「翻页找盘」走不通，只能靠 sitemap + 拼音前缀定位。
   - **覆盖不均**：本次 10 个目标盘中贝壳收录 7 个，其中仅 3 个有 `hdic-frame` 户型图；绿城·臻园/花城壹号院/璞樾墅虽被收录但户型图位为空；中海熙园、岭南1号根本未收录（只撞到同名干扰项"岭南V谷"）。**以上「无户型图」的盘，可按 §2.d.9 去小红书试线索与字段（但别指望在那里拿到户型图），总图改走 §2.d.2 政府批后公布。**
   - ⚠️ 贝壳详情页**不支持 HTTP Range**（`-r 0-6000` 仍返回全量 200），别指望只取 `<head>` 省流量。
8. **「在售」是强时效状态**：同一项目在不同稿件里可能一处写「待入市」、一处写「已入市半年」（本次云栖江麓即是）。**一律以政府预售/网签口径为准**，并在笔记中标注核验日期。
9. **小红书 = 销售期住宅的「线索与字段」通道，**不是**可靠图源（2026-09-15 实测打通并当场证伪，脚本已内置）**：
   销售期楼盘在门户（吉屋/贝壳/房天下）的户型图位**大面积为空**，小红书上中介/踩盘号数量最多、更新最快，因此**值得第一时间去搜**；但实测结论必须修正为：**它补得出「字段」，补不出「户型图」**。要点：
   - **定位**：`bsk navigate https://www.xiaohongshu.com/search_result?keyword=<url编码>`；结果卡片 `.note-item`，标题在 `.title`，**有效链接必须取 `a.cover` 的 `href`**（形如 `/search_result/<id>?xsec_token=…&xsec_source=`）。⚠️ `a[href^="/explore/"]` 是 **0 尺寸 SEO 壳**，点不到也取不到 token；`/explore/<id>` 裸链或 token 过期 → 跳 `/404` 报「当前笔记暂时无法浏览」。
   - **取图**：见 §2.c 实测结论第 6 条（认 `!nd_dft_wlteh_webp_3` 后缀 + Referer，**原样直下**，勿改后缀）。
   - **正文即字段金矿**：笔记 `#detail-desc` 常直接写出**开发商全称、预售证号、总套数、面积段、总价段、代理机构**。本批据此补全绿城·臻园（预售证 `穗房预(网)字第20260156号`；54 席＝26 联排合院+28 叠拼；合院实用 600–1000㎡、叠拼 400–500㎡；地下室层高 6.3m）与花城壹号院（`广州市花都区恒晟房地产开发有限公司`（花都城投旗下）；`穗房字第20260028号`；建面 180–370㎡；总价 500–2000 万；代理 广州市盛业行房地产顾问）。
   - **真伪判据（2026-09-15 修正，此前判据不成立）**：**不能**用标题判断——标题含「户型图/全户型/测评/平面/首发/干货」的笔记，正文仍可能整篇是实景照。**唯一可靠做法是逐张目视核验图面**（见 §2.c 第 7 条，用 `scripts/audit_sheet.py`）。已知的三种「有图等于没图」：① 标题写户型、正文全是会所/景观实景；② 真贴了户型但**被发布方主动打码**；③ 户型只做成**海报底部的极小缩略图**（单体约 170×260px），只能当线索、不能当图纸。中介卖点帖通常**只有 1 张封面**、正文是价格+地铁+学区话术 → 封面**不得标为户型图**。
   - **正确的渠道分工（本批 33 张户型图的真实出处）**：`贝壳新房 hdic-frame` 17 张 + `吉屋 /housetype/` 16 张 = **全部**；小红书与公众号贡献 **0 张**户型图，但贡献了**总图级素材**（公众号原文鸟瞰总图）、**沙盘照**与大量有效字段。⇒ **户型图先走贝壳/吉屋，总图走市规资局批后公布 + 相册审批版鸟瞰，小红书用来补字段、找线索、兜底试探。**
   - **现成脚本（本 skill 自带，直接调用，勿重写）**：`scripts/xhs_scrape.py`（给定笔记 URL 列表批量取图）、`scripts/xhs_search.py`（关键词搜索 → 命中筛选 → 自动逐笔记访问 → 自动落图 + 生成 `raw/xhs_digest.md` 文案摘要）。用法见脚本顶部 docstring；两者均依赖已连通的 browser-skill 会话（`bsk session list` 取 session id 传入）。
10. **微信公众号 = 销售期住宅的「字段 + 总图/鸟瞰/沙盘」通道（2026-09-15 实测打通，脚本已内置）**：
    - **入口**：搜狗微信 `https://weixin.sogou.com/weixin?type=2&ie=utf8&query=<url编码>`（`type=2` = 文章）。结果条目在 `.news-list li`，标题 `h3 a`，日期 `.s2`。
    - **跳转（关键，走错必被拦）**：条目 `href` 是加密的 `/link?url=…&token=…` 且**有时效**。
      - ❌ 直接 `bsk navigate` 到该 `/link` 地址 → 常落 `antispider` 校验页（这正是 §2.d.6 旧结论的来源）。
      - ❌ `bsk click` 也不行：条目 `target=_blank`，新标签会被浏览器拦掉、主标签不动。
      - ✅ **正确做法**：先 `navigate` 到该 query 的搜索结果页，再用页内 JS 跳转
        `location.href = document.querySelectorAll(".news-list li h3 a")[idx].href`（同站 referer + cookie 齐备），等待 4–5s 即落 `mp.weixin.qq.com`。
    - **取图**：正文图在 `#js_content img`，真地址在 **`data-src`**（懒加载，`src` 只是占位）。域名 `mmbiz.qpic.cn`**非签名**：把 URL 的 **`/640?` 改成 `/0?` 即原图**（实测 1080 宽 → 2276 宽），带 `Referer: https://mp.weixin.qq.com/` 用 urllib/curl **直下即可，不必过浏览器**。图文里的 `mmbiz_gif` 是动图，仍可下但归类时按海报处理。
    - **字段金矿**：`#js_content` 正文常直接写出**产品结构（上叠/下叠层数、入户动线）、容积率、得房率、面宽与层高、总价段、网签成交、组团关系**。本批据此补出金茂越秀·璞樾墅：**抬地坡地「上合墅 5 层 / 下合墅 4 层」、各自独立院门与入户动线、容积率 1.1、约 10.8m 南向面宽、层高约 3.5m、≥200% 得房率、总价 2000–3800 万**；同文配图还给出 19# 上叠网签 **331.3㎡ / 18.99 万·㎡ / 6259.2 万**。
    - ⚠️ **最大的坑：标题带「户型图」≠ 真有户型图**。占比最高的是**「售楼处电话营销贴」**——标题堆砌「户型图-容积率-最新价格」，正文却是 400 电话 + 卖点 + 效果图 + 航拍。本次实测该篇 22 张图**逐张核验后户型图 0 张**，但**总图/鸟瞰级素材 3 张**（世界大观四组团鸟瞰、配套分布示意图）确实值得入库。⇒ **图片一律按 §2.c 第 7 条逐张目视核验后再归类，不得凭标题。**
    - **检索技巧**：搜狗分词**很粗**——搜「璞樾墅户型图」，10 条里 8 条是北京楼盘（把「墅」当词根召回）。对策：① 用**项目全名**（含开发商前缀，如「金茂越秀璞樾墅」）；② 结果仍必须**按项目名做别名过滤**（脚本已内置 `aliases`）；③ 加「户型图」这类泛词会**稀释召回**，可换「<项目名> 户型 解析」「<项目名> 上叠 下叠」；④ **同名楼盘必须加 `exclude` 词表**（城市/板块，如绿城臻园需剔除 台州/上海/唐镇/西安）——脚本已内置 `exclude`，否则异地同名项目会大量污染结果（本批绿城臻园首次检索 0 命中、加排除词后命中）。
    - ⚠️ **跨号重复图**：同一张渲染图常被不同公众号**换水印重复发布**。入库前必须比对**像素尺寸 + 感知哈希 aHash**（汉明距离 <10 判同图），**只保留高清版本**（本批岭南1号 673×345 与 1290×660 两张实为同图）。**不要**仅凭文件名不同就当成两张素材计数。
    - **图片命名与 wikilink 安全**：文件名**禁用 `#` `|` `[` `]` `^` `{` `}`**——`[[...]]` 中的 `#` 会被 Obsidian 解析为标题锚点、`|` 为别名分隔，含之则链接失效（本批 `006-…楼栋1-9#.jpg` 踩中，已改名）。
    - **公众号身份可复用**：文章页可读 `#js_name`（号名，如本批命中的「粵房售」）。**开发商官号 > 中介/自媒体号**，图纸质量差别很大 → 拿到官号名后可定向再搜该号同项目推文。
    - ⚠️ **号名会被脚本截断为 10 字符**（2026-09-21 实测）：下载文件名前缀取的是 `#js_name`，但脚本里做了 `re.sub(r'[^\w]','',acc)[:10]` 截断——`金牛高新中海信成都创新产业城` 实际落盘为 `金牛高新中海信成都创新产_21.png`、`新港产投-仙林湖智造园` 落为 `新港产投仙林湖智造园_13.png`。**归档脚本写源文件名时不能按完整号名猜，必须先 `listdir` 读出真实文件名**，否则整批 `MISS`。归档目标名仍按**项目名**还原，不复用截断前缀。
    - **现成脚本**：`scripts/wx_search.py`（多关键词 → 结果过滤 → 逐篇页内跳转打开 → 抽正文与图 → 落 `raw/wx_articles/*.json` + 下载 + 生成 `raw/wx_digest.md`）、`scripts/wx_dl.py`（给定 URL 列表下载）、`scripts/wx_from_article.py`（从文章快照 json 下载）、`scripts/sheet_any.py`（任意目录拼带编号审图板）。用法见各自 docstring；依赖已连通的 browser-skill 会话。

### 2.e 产业园区/厂房类主题（2026-09-21 实测，与住宅类、建成案例类都不同，先读）

> 适用：「产业园区」「独栋厂房」「研发办公独栋」「产业载体」「厂房户型图」这类主题——
> 主体是**开发商/运营方**（同 §2.d），但**来源结构**和住宅类完全不同，不能套用贝壳/吉屋那套。

1. **渠道产出排序（本批 39 张图 / 22 张真平面图的真实出处）**：
   `园区/开发商微信公众号正文`（**唯一高产，本批 22 张真平面图 100% 来自这里**）`>` `开发商自建门户 / 招商门户（招商800 等，偶得真图）` `>>` `中工招商网 / 厂房网 / 安居客 / 房天下 等产业门户（≈0 张）`。
   ⚠️ **产业门户的「户型图」栏目普遍以渲染图充数**——本批实测 `incham_*` 11 张全为纽约天际线、二维码、抽象科技图等装饰素材；门户「户型图」栏目标题下的图多为效果图。⇒ **不要把产业门户当图源主力，直接主攻公众号。**
2. **「投资回报/招商卖点」类文案挂的图基本不是图纸**：这类稿件配图是实景、鸟瞰、区位图、业态联盟图、二维码。**只有「产品介绍」「户型鉴赏」「产品推介」「空间展示」类标题的推文才可能含真平面图**——检索时优先用这些词根组合项目名。
3. **公众号检索词策略（与住宅类不同）**：住宅类要**项目全名**收敛；产业类项目名往往冷门，**用「<项目名> + 产品/户型/平面」比裸项目名命中率高**；另一条高效路径是**直接用泛词搜行业推文**（如「工业厂房 标准层平面图 柱网」「产业园 独栋 首层平面图 标准层平面图」），能从「户型优质楼宇推介」「【户型鉴赏】XX户型」这类**逐户型连载推文**里成批拿到图纸——本批「华创企业独栋A1/B1户型」即由此发现。
4. **`#js_name` 截断坑**：见 §2.d.10 末条，产业类号名普遍很长（`金牛高新中海信成都创新产业城`、`新港产投-仙林湖智造园`），**必踩**。
5. **字段密度**：产业独栋的**面积段/层高/荷载/柱距/电梯配置在正文文字里比图面更全**，且常以「产品参数表」形式成组出现；图面往往只有平面轮廓。⇒ **图抓图、字抓字，两处都要取，不要只取图。**
6. **政府规划批后公布（§2.d.2）对产业园区同样有效**（坐落地块的总平面），但**拿不到单体户型平面**——户型平面仍只能靠公众号。
7. **平面图可得性必须逐项目判定并显式标注**：本批 9 个入选项目中，**7 个有清晰平面图、2 个只有总平/产品分布图**。产品分布**轴测图**是产业类特有的高价值素材（一图可含多档产品的面积段+层高+荷载，如仙林湖 `001-产品分布轴测图`），**要单独归类、当作"功能分区示意"交付，不得冒充户型平面**。
8. **普遍缺失的字段 = 功能面积配比**：本批 9 个项目**无一**给出「研发/办公/生产/仓储面积配比」。该口径在产业类公开资料中基本不存在，只有行业论坛/咨询号讲**范式**（如「1F 重生产 / 2F 轻生产 / 3F 仓库 / 4F 办公」的垂直分区）。⇒ **留空并在回执说明，不要用范式值填充具体项目字段**。

## 5.a 图源不可抓时落为文字档案

当某 B 组项目的图源在 §2.a 顺位全失败（403/404/懒加载/反爬等），不删该案例、不降级骗自己——按下列模板落"文字档案"型笔记：

1. frontmatter **仍写满 12 字段**（便于 Dataview 视为正常笔记）：`cover` 留空、`tags` 仍按主题填、`aliases` 给原英文名；
2. 正文概述段先于图片段——把可读到的事实字段（建筑师/年份/面积/业主）写进"基本信息"；
3. `## 图片` 段直接省略，或写为「待补图：来源 URL 失效，建议补路径 <事务所官网/微信公众号转译>」；
4. `## 为什么值得收录` 与 `## 关键设计特征` 段照常写（这是档案的真正价值）；
5. ⚠️ 在正文末段明确：「⚠️ 图源不可抓：<来源 URL> 返回 <403/404/懒加载>；故无本地图片与 cover。建议后续从事务所官网或国内媒体补图」；
6. `## 参考资料` 段只放原失败 URL（不删，留作日后回溯线索）。

文字档案与有图笔记同样进入 vault、不另设文件夹、不另起 frontmatter 类型。回执「字段冲突与待核实」段单独列一段「无图项目」，写明每个项目的来源 URL、补图建议与最近抓取失败码。

## 3. 筛选（SOP ④）

**硬过滤**（直接出局）：类型不匹配词表 / 地域不符 / 年代超范围 / 纯付费墙或无图 / 信息量过少 / 明确低质转载。

**评分**（百分制）：

```
得分 = 来源可信度 ×35% + 任务相关度 ×30% + 素材完整度 ×25% + 新鲜度 ×10%
```

- 可信度：命中最高级别给基础分——`S=100 / A=85 / 公众号·小红书=75 / 百科·索引(C)=40 / 线索=20`，多源取高。注：若公众号为**事务所官方认证号转载或首发**，按 A 级（85）记分（同源最高档）；小红书未署名拼贴按线索（20）计。
- 任务相关度：主题关键词在素材中的命中率 + feature 可归纳强度；有 `pref` 时命中偏好 +10~20
- 完整度：平面/剖面/细部图、面积年份等硬数据、署名、多图
- 新鲜度：建成越近越高（非硬性）

| 得分 | 处理 |
|---|---|
| ≥ mode 阈值 | 正式收录 |
| 40–69 | 候选清单（回执列一行 + 未达线原因） |
| <40 | 丢弃，回执记录原因 |

**多源合并**：同案例多源命中 → 合并为一条，图片/字段取并集去重；关键字段冲突 → 按可信度裁决链 **S > A > 公众号·小红书 > 百科 > 线索**（即字段冲突时取更可信档的值，并参照 §2 第 1 条在正文 ⚠️ 标注双方口径），仍无法裁决 → 正文顶部加 `> [!warning] 待核查：…` callout，`status` 置 `待核查`。

**候选确认闸口**：`confirm` 开启时，产出一份**可视化候选清单**（`staging/<批次名>/候选清单.html`，相对路径引用候选题图），并 present 给用户确认后才进入落库。清单必须包含：

1. **题图卡片**：每个候选一张题图（§2 第 3 步已下载），卡上标注 项目名 / 建筑师 / 建成年份 / 地点 / 得分 / 来源级别；无题图者用占位块并注明原因。聊天中同时附纯文字表格便于快速回复选择。
2. **库内查重标注**（候选阶段即查，不等 ⑤）：对每个候选用 **项目中文名、英文名/别名、建筑师+城市** 三个键，在 `<vault>/01 - 案例库` 全库检索（文件名、frontmatter 的 title/aliases/architect/location、正文首行）。命中 → 该候选归入清单底部「**库内已有**」分组，附旧笔记相对路径链接，**默认不计入收录建议**；仅当用户明确说「替换/深挖/仍要收录」时才解禁。未命中 → 标「新案例」。
3. **分组呈现**：`建议收录`（≥阈值）/ `候选`（40–69）/ `库内已有`（查重命中）三组，附一句推荐理由。
4. **素材计数必须标注「已核验」**（2026-09-15 增）：清单中的素材数字（户型图 / 总图 / 楼盘图）**只能来自逐张目视核验后的归类**（§2.c 第 7 条），并显式写明「已逐张视觉核验」；未核验的图片一律标为待核、不计入。缺图项目须在卡片上直接标出「无户型图 / 无总图」，**不得用门户栏目名或笔记标题充当分类依据**。图表统计应把「户型图」「总图／鸟瞰」**分开计数**——这两类是本类主题的核心交付物，混在一起会掩盖缺口。

**确认后的题图复用**：用户选定收录的候选，其候选题图**直接移入** `attachments/Clippings/<ProjectName>/001-*.jpg` 作为 cover（重命名为规范名），避免重复下载；后续补图从 002 顺延。

## 4. 归类定名与查重（SOP ⑤）

1. `type` 必须取规范 §3 受控长词（文化建筑/体育建筑/商业综合体/教育医疗/住宅居住/产业建筑/城市设计），并已与用户确认；
2. 目标文件夹 = `01 - 案例库/<type>/`；新类型文件夹出现时提醒用户去 MOC「分类目录」补行 + 标签体系登记（skill 不代改）；
3. 文件名 `{项目名}，{城市} - {建筑师}.md`（全角逗号；城市用二级地名；建筑师署名事务所）；英文原名进 `aliases`；
4. **查重（防御性复查）**：候选阶段已全库查重（§3），此处仅在**文件名最终定名后**再核对一次目标文件名是否已被占用。命中 → 转入回执提示用户是否并入，**绝不新建、绝不改写**，等用户指示。

## 5. 生成与写入（SOP ⑥）—— vault 写入安全机制

**总原则：只增不改（additive-only）**。vault 内仅允许新增：案例笔记、类型文件夹、**专题研究文件夹与专题笔记**、图片目录、搜集回执。

**永不触碰**：`00 - Meta/标签体系.md`、`00 - Meta/模板/`、任何已有笔记的正文、`.obsidian/`、**任何 MOC 的 Dataview 查询块**。

> **唯一例外（2026-09-15 经用户明确要求后确立）**：`06 - 专题研究/MOC - 专题研究.md` 的「**专题目录**」手工列表与「**标签规范**」表格**允许补行**（新增专题时须登记，见 §6.b）。除这两处外 MOC 其它内容一律不动，**查询块一个字符都不许改**（§7 红线 3）。

写入四步，缺一不可：

1. **暂存**：笔记与图片先写 `staging/<批次名>/`（vault 零改动）。图片按规范 §8 放 `attachments/Clippings/<ProjectName>/001-*.jpg` 结构暂存，精选 5–20 张，cover = 最具代表性一张 = 正文首图。
2. **自检**：逐篇对照 `assets/checklist.md` 打勾；任一项不过 → 该案例退回候选，不入库。
3. **原子落库**：自检通过后逐文件复制进 vault——先写 `<目标名>.tmp`，读回校验，再重命名为最终名。单文件失败只记录该文件，不中断批次。
4. **落库验证**：读回每个新文件，确认 frontmatter 可解析、`cover` 指向的图片已存在；失败 → 删除该新文件并在回执标注。

**笔记格式**：严格按 vault 规范 §5.2「快速收录」档——frontmatter **只允许 12 字段**（title/date/architect/year/location/type/status/rating/feature/cover/tags/aliases），面积/结构/业主等细项只写正文「基本信息」；`status` 新建一律 `待整理`，`rating` 留空；来源未提的字段留空，**禁止臆造**。可选补 `## 平面与空间`、`## 材料与构造`、`## 获奖`。

**标签**：新标签先在 `00 - Meta/标签体系.md` 全文检索查重；查不到的同义标签**不自动创建**，登记到回执「待登记标签」交用户处理。

**单主题素材不足**（正式收录 <2 篇但主题值得留档）：按规范 §3 合集约定生成 `{主题} - 参考合集.md`（`type` = 主导类别，正文 `## 参考项目（部分）`）。

## 6. 收尾（SOP ⑦）：搜集回执

写 `<report_to>/YYYY-MM-DD-<主题>-搜集回执.md`，内容：本批写入的**完整文件清单（含 vault 相对路径，供回滚）**、收录/候选/丢弃统计及原因、**候选阶段库内查重命中记录（项目名 + 命中的旧笔记路径 + 用户处理方式）**、来源覆盖情况（哪些来源未覆盖）、待登记新标签、browser-skill 使用情况与遇险记录。

落库全部完成后清理 `staging/<批次名>/`（保留亦可，回执中注明）。

## 6.a 归档位置规范（**先读这条，别搞错地方**）—— 2026-09-15 血的教训

**本 skill 的一切产出默认归档进 Obsidian vault，不是 WorkBuddy 资料库。**

用户的知识库是 `<vault>`（`U:\DK_iCloud\...\Arch_Vault`，见 §0）。**资料库（`workbuddy.cn/space`）是另一套独立系统**——只有用户**显式点名**时才写它（说「资料库」「workbuddy 空间」「在线文档」，或直接给出 `space` 链接）。

按批次性质分流：

| 批次性质 | 归档位置 | 依据 |
|---|---|---|
| **建成案例**（建筑师署名、S/A 级来源） | `01 - 案例库/<type>/` ＋ `attachments/Clippings/<ProjectName>/` | vault 规范 §3 / §4 / §5.2 |
| **专题研究 / 产品研究 / 销售期楼盘产品资料**（开发商主体、门户与政府来源、强时效） | **`06 - 专题研究/<专题名>/`** ＋ `attachments/Clippings/<项目名>/` | vault `MOC - 专题研究` 的命名规范 |
| 用户显式点名「资料库」 | WorkBuddy 资料库 Database | §6.c |

**踩过的坑（2026-09-15，本 skill 真实失误）**：用户说「放到**专题研究**里的**住宅产品研究**里」，我理解成 WorkBuddy 资料库的目录，于是在资料库新建了「专题研究/住宅产品研究」两级文件夹 + 一张案例表 —— **方向完全错了**：vault 里本来就有 `06 - 专题研究` 这一层（含 `产业研究/` 等既有专题与 `MOC - 专题研究.md`），我却建成了平行的一套。

> **判据（务必执行）**：接到「放到 X 里的 Y 里」这类指令，**先 `ls` vault 根目录**，看有没有同名层级的**现成目录**；有就写它，**不要另起炉灶**。用户说的目录名，大概率就是他 vault 里已有的结构。

## 6.b 专题研究类批次的 vault 归档流程（2026-09-15 实测跑通）

目标结构（**沿用 vault `MOC - 专题研究` 的命名规范**）：

```
06 - 专题研究/<专题名>/
├─ <专题名>.md                 ← 专题主笔记：topic / status / started / updated / tags
└─ <批次研究正文>.md            ← 本批成果。⚠️ frontmatter「不加 topic、不加 status」
```

> ⚠️ **子笔记的 frontmatter 只写 `tags`**。`MOC - 专题研究` 的两块 Dataview 是 `WHERE topic != null` 与 `WHERE status = "进行中"`——子笔记若带 `topic` 会被「专题一览」重复列出，若带 `status: 进行中` 会污染「进行中的专题」。

**执行顺序**：

1. **素材归档**：改 `scripts/to_vault.py` 的 `PLAN` 表（`(项目名, 源目录key, [(源文件名, 目标文件名)])`），跑 `--dry` 看清单，再正式执行。脚本按 vault 写入安全机制走「写 `.tmp` → 读回 md5 校验 → 原子改名」，且**目标已存在则跳过、绝不覆盖**。
   - **补轮次（第二轮及以后）不要改 `to_vault.py`**，另存 `to_vault2.py` / `to_vault3.py`，`PLAN` 写成 `(项目名, 源目录key, 起始编号, [...])`，**新编号一律追加在既有最大编号之后**——这样**既有文件名一个都不动**，不会破坏已发布的笔记引用。
2. **写笔记 2 篇**：专题主笔记 + 本批研究正文（正文内用 `![[attachments/Clippings/<项目名>/NNN-xxx.jpg]]` 嵌入）。
3. **登记 MOC**：在 `<vault>/06 - 专题研究/MOC - 专题研究.md` 的「**专题目录**」补一行 wikilink；若有新 `research/*` 标签，在「**标签规范**」表补一行。⚠️ **只改这两处手工列表/表格，Dataview 查询块一个字都不许动**。
4. **写搜集回执**：`00 - Meta/搜集回执/YYYY-MM-DD-<主题>-搜集回执.md`（见 §6）。
5. **落库验证**：读回每篇笔记（frontmatter 可解析）、用正则核一遍全部 `![[...]]` 引用**逐个存在**。用 `scripts/verify_vault.py`（核 vault 笔记的全部 `![[...]]` 是否命中 + frontmatter 可解析）与 `scripts/verify_list.py`（核 `候选清单.html` 的 `img/` 引用是否命中）。**必须同时满足「引用 0 缺失」与「vault 图片 0 未引用」两个计数**——后者能揪出「归档了但忘了嵌进笔记」的漏网图。

**图片命名以图面内容为准**：`NNN-<图面内容>.jpg|png`（如 `001-下叠B1b户型163.6㎡.png`）。**禁止**用来源标题或门户栏目名命名——本批据此把「按标题归类」的虚报全部改正。归档**前**先用 `scripts/audit_sheet.py`（整目录）或 `scripts/board_new.py`（给定清单，配 `raw/new_files.txt` 每行 `项目key/文件名`）生成带编号审图板逐张核验。

**⚠️ 文件名禁用字符（Obsidian wikilink 会失效）**：`#  |  [  ]  ^  {  }`。
`[[...]]` 里的 `#` 会被解析成**标题锚点**、`|` 会被解析成**别名分隔**、`[]^` 亦有语义 → 文件名含这些字符时 `![[attachments/.../006-xxx·楼栋1-9#.jpg]]` **链接直接失效**。命名前扫一遍，把 `#` 换成「号 / 至」，`|` 换成「·」。归档后自查：`[c for c in fname if c in '#|[]^{}']` 应恒为空。

**跨渠道 / 跨号重复图去重（必做）**：同一张渲染图常被不同账号**换水印重复发布**（本批岭南1号、绿城臻园各中招一次）。判据两条同时看：① **像素尺寸**（保留高清版）；② **感知哈希 aHash 汉明距离 <10**（`Image.resize((8,8)).convert('L')` 后比均值位）。本批岭南1号 673×345 与 1290×660 两张实为同图 → 只保留后者。

**专题研究 vs 案例库的分工**：产品研究类材料主体是**开发商/产品线**、状态随开盘去化而变，与案例库「建成作品 + 建筑师署名」口径不同，**不进 `01 - 案例库`**，两者互不替代。

## 6.c 仅当用户显式点名「资料库」：归档进 WorkBuddy 资料库（2026-09-15 实测跑通）

用户明确说「存到资料库」「放到 workbuddy 空间」时走这里。**资料库与 vault 是两条并行线**：vault 存笔记与图片，资料库存**结构化汇总表**（Database，一案例一行，便于筛选/对比/评分排序）。

**执行顺序**：

1. `scripts/dump_cases.py` → 生成 `raw/cases_dump.json`（案例字段 + 磁盘上 `HT/ZZ/POSTER/ALB` 各分类实况计数）。
2. 在资料库里建目录树：`manage/create_folder.py --title "专题研究"` → 再用返回的 `nodeBlockId` 作 `--parent-id` 建子文件夹。
3. `scripts/push_to_library.py <token> create` → 建 Database（顶层 schema 带 `space_id` + `parent_id` 直接落到目标文件夹下）。
4. `scripts/push_to_library.py <token> records <database_id>` → 批量写记录。
5. 只读验收：`query_database_record.py` 按 `收录得分` 降序拉一遍，核对条数/分数/图片计数。

**字段口径（沿用本 skill 的评分与分类，不要另起一套）**：项目名称 / 所在区域 / 板块·区位 / 产品类型(multi_select) / 设计·开发单位 / 入市·交付节奏 / 面积段 / 产品特点 / **收录得分** / **收录状态（建议收录·待补图候选）** / **户型图(张)·总图鸟瞰(张)·楼盘图(张)** / 已有可用户型图(checkbox) / 来源通道 / 字段冲突与待核事项 / 核验日期。**收录状态 = 得分 ≥70 且 户型图 ≥1** 双条件；素材计数必须是**逐张视觉核验后**的数（§2.c 第 7 条）。

**接口要点**：`mode=client` 时先 `connect_open_platform(skill_id="library")` 换票（**有效期 30 分钟，超时报 `code=NETWORK_ERROR`，换票后重试即可，别误判为网络故障**）；`create_database.py` / `batch_add_database_records.py` 均支持 `--stdin`（**首行 token、其后 JSON**）；`space.workspace.list-node` 是单数（写成 `list-nodes` 会返回空）。本机 bash shim 缺 `dirname`/`head`/`cat`，**不要用管道接 head/cat**，直接用 python 驱动脚本。

> ⚠️ **删除限制（重要，事前就要跟用户说清楚）**：资料库**没有节点删除 API**——`space_api.py` 只提供 `move-node` / `rename-node`，**文件夹与 Database 节点都删不掉**，只能用 `space.database.batch-delete-records` 删**记录**。所以**误建的结构无法由 Agent 收拾干净**，必须让用户在前端手动删除。⇒ **写之前先确认位置，别依赖「错了再删」。**


## 7. 硬红线（违反任意一条即停止本轮并向用户报告）

1. vault 路径不可达 / 只读 / 磁盘满 → 中止，**绝不降级写到别处**；
2. 绝不覆盖、修改、删除、移动 vault 内任何已存在文件；
3. 绝不手写或修改 MOC 查询块；
4. 绝不臆造事实字段；
5. 绝不写入 frontmatter 规定 12 字段之外的结构化字段。
